import * as apis from '../../api';

const state = {
  all:[]
}

const mutations = {
  SET_TASK_LIST (){
    
  }
}

const actions = {
  getListData (context){

  }
}

export default{
  state ,
  mutations,
  actions
}